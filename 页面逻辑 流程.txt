开始时间

（改成读取核心数据文档）
指定原始数据的文档名称（在外层文件夹），（用函数）把它读成行为单位的数组

遍历读取行为单位的数组，跳过第一行（各列标题），做出历史数据要循环进行处理形成比较的核心数据的数组（可以循环写入指定正在处理的文档），并统计出数据数组的数量

鼓励语句数组，盒子

其他的提示信息栏：

制作各种session

创建按钮--根据session路径，创建文件夹和文档

点击--产生一组随机数据，并且数组内的数据间不能有相同（这是肯定不允许的），然后自动判断该数组历史是否已经出现过，出现过几次，（目前表明历史数据竟然没有重复过！）

产生随机数组的函数

操作数据的各种操作动作的表单
遍历存放数据的全部文件的文件夹

比较数据表单：
历史数据，此次结果，上次结果
遍历统计记录重复的数组，输出结果中重复数据的重复情况----结果


输出运行结束时的时间
计算总运行时间